{
  "name": "paninifs-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "E2E tests for paninifs.org using Playwright",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test e2e/tests/smoke.spec.js",
    "test:research": "playwright test e2e/tests/research.spec.js",
    "install:pw": "playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0"
  }
}
